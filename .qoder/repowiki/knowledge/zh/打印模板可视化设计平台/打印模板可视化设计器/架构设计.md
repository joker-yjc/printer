- 采用三栏式布局架构：左侧资产面板（AssetPanel）、中间核心画布（Canvas）、右侧属性/树面板（PropertyPanel/ComponentTreePanel）。
- 依赖 `useDesignerStore` 作为跨模块通信的唯一事实来源，实现组件拖拽、选中态同步及 JSON Schema 生成的解耦。
- 顶层容器负责处理路由参数加载、全局缩放控制及调试视图的悬浮叠加，确保各子模块专注于局部交互逻辑。