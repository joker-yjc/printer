- 采用三栏式布局架构，以 `useDesignerStore` 为唯一事实来源（Single Source of Truth），实现左侧资产面板、中间核心画布与右侧属性/组件树面板的数据同步。
- 各子模块（AssetPanel, Canvas, PropertyPanel）作为纯视图组件，不直接通信，而是通过订阅和派发 Store 动作完成跨模块交互（如拖拽添加、选中编辑）。
- 顶层容器负责处理路由参数加载、全局缩放控制及调试信息的聚合展示，确保核心业务逻辑与基础设施解耦。