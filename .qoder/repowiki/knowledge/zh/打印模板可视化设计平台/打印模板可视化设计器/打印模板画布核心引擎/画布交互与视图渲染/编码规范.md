- 在 `useEffect` 或事件监听器中，使用 `useRef` 同步最新的状态值（如 `pageConfigRef`, `zoomLevelRef`），确保异步回调能获取到最新的上下文数据。
- 所有涉及物理尺寸的坐标转换均通过 `pxToMm` 工具函数统一处理，并结合 `snapToGrid` 实现基于网格或 Shift 键临时禁用的吸附逻辑。
- 组件越界检测逻辑在 `index.tsx` 和 `SaveTemplateModal.tsx` 中保持一致，针对页头、页脚及连续纸模式分别计算不同的边界约束。