- 渲染器组件统一采用 `{ComponentName}Preview` 命名规范，并定义为接收 `component: ComponentNode` 属性的函数式组件。
- 属性访问普遍采用可选链与空值合并运算符（如 `component.style?.fontSize || 14`）以确保在配置缺失时提供安全的默认回退值。
- 尺寸单位转换遵循固定系数逻辑，在涉及物理尺寸（mm）到屏幕像素（px）转换时，常使用 `* 3.78` 进行近似换算。