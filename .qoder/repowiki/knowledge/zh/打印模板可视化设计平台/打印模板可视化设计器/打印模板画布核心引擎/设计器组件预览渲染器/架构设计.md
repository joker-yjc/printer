- **统一接口契约**：所有渲染器均接收 `ComponentNode` 作为唯一输入，通过读取其 `props`、`style`、`layout` 和 `binding` 属性驱动 UI。
- **扁平化导出架构**：通过 `index.ts` 集中导出各组件预览器（如 `TextPreview`, `TablePreview`），供上层画布引擎按需调用。
- **差异化实现策略**：简单组件（文本、矩形、线条）采用纯 CSS 样式映射；复杂组件（二维码、条形码）封装并复用底层业务组件；表格组件集成 `print-sdk` 进行列宽计算及拖拽交互逻辑。