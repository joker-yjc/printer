- 属性面板采用插件化设计，通过 `StylePlugin` 接口定义不同组件类型的样式配置 UI，并在 `stylePlugins/index.ts` 中统一注册。
- 画布交互逻辑（如拖拽、缩放）大量使用 `useRef` 缓存最新状态，以避免在频繁触发的 `mousemove` 事件中产生闭包陷阱或重复绑定监听器。
- 组件类型映射遵循统一约定，在预览渲染器 (`componentRenderers`) 和属性面板中均通过类型字符串（如 'text', 'table'）进行分支处理或插件匹配。