- 采用主从组件架构：`index.tsx` 作为容器负责数据获取（通过 `templateApi`）与状态管理，`TemplateFormModal` 和 `TemplatePreviewModal` 作为展示型子组件处理表单交互与信息展示。
- 依赖外部服务层：通过 `designer/src/services/api` 抽象后端接口，支持基于环境变量的 Mock/真实 API 切换。
- 路由集成：利用 `react-router-dom` 实现从列表页到可视化设计器页面的导航跳转。