- **全局状态驱动**：采用 `zustand` 构建单一事实来源（`designer.ts`），统一管理画布组件树、页面配置及操作历史，实现管理页与设计器间的数据同步。
- **路由分层隔离**：通过 `react-router-dom` 区分全屏沉浸式设计模式（`/designer`）与带布局的管理后台模式（`/templates`, `/schemas`），确保不同任务场景的UI专注度。
- **区域化渲染契约**：定义页头、内容、页脚三大逻辑区域，所有子模块（画布、属性面板、预览）均基于此区域模型进行组件的定位、约束校验及最终JSON序列化。