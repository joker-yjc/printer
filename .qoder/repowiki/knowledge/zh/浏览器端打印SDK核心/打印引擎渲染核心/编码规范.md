- 所有渲染器类均实现 `ComponentRenderer` 接口，强制包含 `type` 标识、`render` 方法及可选的 `calculateHeight` 方法。
- 统一使用 `buildPositionStyle` 工具函数处理毫米到像素的坐标转换，确保不同分辨率下的打印一致性。
- 组件样式构建采用“常量默认值 + 用户配置覆盖”的模式，优先从 `constants.ts` 读取基础样式。
- 表格渲染器内部实现复杂的列宽自适应算法（`computeColWidths`），支持固定列宽与剩余空间均分的混合布局。