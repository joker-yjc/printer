- 使用 useRef 缓存 Store 状态（如 components, pageConfig）以避免 useEffect 依赖频繁更新导致的事件监听器重复绑定。
- 在拖拽结束（mouseUp）时通过计算位移阈值（dx > 3 || dy > 3）区分点击选择与真实拖拽操作。
- 对页头、页脚及内容区域实施统一的边界检测逻辑，并在组件超出所属区域时应用特定的 CSS 越界样式（红色虚线）。
- 所有模态框（保存、设置）的状态管理均集中在父组件 index.tsx 中，通过 props 传递 open 状态及回调函数。