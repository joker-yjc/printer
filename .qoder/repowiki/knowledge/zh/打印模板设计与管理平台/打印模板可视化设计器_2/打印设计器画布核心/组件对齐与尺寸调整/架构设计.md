- 逻辑层：`alignmentDetector.ts` 负责基于毫米坐标计算组件间的对齐关系（垂直/水平）及吸附建议，支持缩放级别转换。
- 表现层：`AlignmentGuides.tsx` 根据检测结果渲染动态参考线；`ResizeHandles.tsx` 实现八向拖拽手柄，处理鼠标事件并实时计算布局变更。
- 样式层：通过 CSS Modules (`*.module.css`) 隔离组件样式，定义参考线动画及手柄交互状态。
- 依赖方向：表现层组件依赖逻辑层的类型定义与工具函数，整体通过 `onResize` 等回调与外部画布状态管理解耦。