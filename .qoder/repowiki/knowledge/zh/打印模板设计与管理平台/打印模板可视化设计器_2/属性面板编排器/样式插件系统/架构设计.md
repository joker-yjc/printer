- 核心接口：`types.ts` 定义 `StylePlugin` 契约，包含 `name` 标识与 `render` 方法。
- 注册机制：`index.ts` 维护全局 `Map` 注册表，提供 `registerPlugin` 和 `getPlugin` 方法，并在模块加载时自动初始化已知插件。
- 策略实现：各具体插件（如 `TextStylePlugin`, `TableStylePlugin`）实现统一的渲染逻辑，通过回调函数将 UI 变更同步至组件状态。
- 容错设计：当请求的组件类型未注册时，系统自动降级使用 `DefaultStylePlugin` 并输出警告日志。