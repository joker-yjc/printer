- 所有业务实体（Schema, Template, ComponentNode）均通过 `src/types/index.ts` 进行强类型定义，确保跨模块数据流转的一致性。
- UI 组件样式采用 CSS Modules（`.module.css`）进行局部作用域隔离，避免全局样式污染。
- API 服务层采用适配器模式，对外暴露统一的 CRUD 接口，内部根据 `VITE_USE_MOCK` 标识自动路由至 Mock 或真实实现。