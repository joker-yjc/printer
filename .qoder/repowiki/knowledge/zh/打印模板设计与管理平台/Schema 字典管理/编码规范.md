- 模态框组件采用受控模式，通过 `open`、`onCancel` 及具体业务回调（如 `onSubmit`）与父组件通信，内部不维护显隐状态。
- 复杂表单提交前执行多重校验，包括 Antd Form 基础校验、JSON 格式解析校验以及业务逻辑校验（如根节点 Key 值约束）。
- 利用 Antd Icons 建立类型视觉映射，在树形预览或列表中根据字段类型（string, number, object 等）动态渲染对应的语义化图标。