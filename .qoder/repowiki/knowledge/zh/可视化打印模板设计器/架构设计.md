- 以 `designer.ts` (Zustand) 为单一事实来源，通过区域感知（页头/内容/页脚）的状态切片驱动左侧资产面板、中间画布与右侧属性面板的联动。
- 采用 React Router 进行功能分区，将核心设计器 (`/designer`) 与管理后台（Schema/Mock/Template）在路由层解耦，共享底层服务接口。
- 定义标准化的 `ComponentNode` 与 `PrintTemplate` 类型契约，确保 UI 组件库（预览/格式化）与设计引擎之间的数据一致性。