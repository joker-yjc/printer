- 采用三栏式布局（Sider-Content-Sider）作为物理容器，通过 `useDesignerStore` 实现跨面板状态同步。
- 左右侧面板支持动态折叠，通过固定定位的悬浮按钮触发，确保画布区域的最大化利用。
- 右下角集成缩放控制与调试入口，提供全局视图调整及 JSON Schema 实时预览能力。
- 右侧面板内嵌 Tabs 切换组件树与属性配置，实现结构导航与细节编辑的逻辑聚合。