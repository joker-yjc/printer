- 使用 `useRef` 缓存 Store 状态（如 `componentsRef`）以解决闭包陷阱，确保全局事件监听器中获取最新数据。
- 在拖拽和调整尺寸时支持按住 `Shift` 键临时禁用网格吸附，提升精细操作的灵活性。
- 对页头、页脚及内容区域实施严格的边界校验，防止组件在跨区域拖拽或保存时出现布局溢出。
- 利用 `sessionStorage` 管理 `ShortcutHint` 的显示状态，确保用户在单次会话中关闭后不再重复打扰。