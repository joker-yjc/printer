- **逻辑层**：`alignmentDetector.ts` 负责核心算法，通过计算组件边缘与中心点的毫米级距离（阈值 0.8mm）生成对齐线与吸附坐标。
- **表现层**：`AlignmentGuides.tsx` 接收逻辑层输出的像素化坐标，在画布上动态渲染垂直或水平参考线。
- **样式层**：采用 CSS Modules (`AlignmentGuides.module.css`) 隔离样式，定义高可见度的洋红色参考线及淡入动画。
- **数据流**：逻辑层完成单位换算（mm -> scaled px）后，将渲染数据单向传递给表现层组件。