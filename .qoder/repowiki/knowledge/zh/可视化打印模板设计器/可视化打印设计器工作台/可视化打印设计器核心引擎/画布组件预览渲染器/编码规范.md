- 所有预览组件均采用无状态函数组件形式，并通过 TypeScript 接口定义以 `ComponentName`Props 命名的属性结构。
- 样式处理遵循“配置优先、默认兜底”原则，广泛使用逻辑或运算符（||）为字体、颜色、边框等属性提供默认值。
- 组件内容获取具有统一的回退路径，通常按 `binding?.path`、`props?.label/text/content` 的顺序尝试读取显示数据。