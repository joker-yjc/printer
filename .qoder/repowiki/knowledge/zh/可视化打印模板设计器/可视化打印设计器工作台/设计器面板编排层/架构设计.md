- 以 `useDesignerStore` 为唯一数据源，三个面板均订阅该 Store 实现状态同步。
- AssetPanel 负责产出新组件实例并注入 Store；ComponentTreePanel 提供结构化导航与右键操作（复制/删除）直接调用 Store 动作；PropertyPanel 根据 Store 中的选中态渲染对应配置项并回写变更。
- 各面板通过 Store 解耦，无直接组件间通信，确保模块隔离性与可替换性。