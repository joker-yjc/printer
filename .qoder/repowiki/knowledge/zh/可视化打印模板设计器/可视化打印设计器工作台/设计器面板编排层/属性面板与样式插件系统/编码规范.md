- 采用受控组件模式，各 Section 通过 `onChange` 类回调将字段变更上报至父组件，由父组件统一更新 Store。
- 样式插件遵循统一接口规范，导出名为 `[Type]StylePlugin` 的常量并实现 `render` 方法，在 `index.ts` 中显式注册。
- 使用 CSS Modules (`index.module.css`) 进行样式隔离，类名通常包含 `property-section`、`property-item` 等语义化前缀。