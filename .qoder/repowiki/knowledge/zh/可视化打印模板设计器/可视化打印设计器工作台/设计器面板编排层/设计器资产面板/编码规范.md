- 子模块通过 HTML5 Drag and Drop API (`dataTransfer.setData`) 传递组件类型或字段路径等元数据，供画布区域接收处理。
- 在添加组件时，若未指定具体坐标，均通过 `getCanvasCenter` 工具函数计算画布中心位置作为默认落点。
- 采用“双击快速添加”与“拖拽精确放置”并存的双模交互范式，并在 UI 上通过 `Tooltip` 提供操作提示。