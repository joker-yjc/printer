- 根目录 `package.json` 定义跨模块工作流（`start`, `build:sdk`, `build:designer`），作为统一的开发入口。
- 采用分层架构：Designer 负责产出 Schema/Template 配置，SDK 负责在浏览器端消费这些配置并执行渲染，两者通过共享的数据结构契约解耦。
- 依赖管理上，Designer 与 SDK 各自维护独立的 `package.json`，根目录仅作为私有工作区容器，不直接参与业务逻辑实现。