- 根目录 `package.json` 定义跨模块生命周期脚本（`start`, `build:sdk`, `build:designer`），作为统一的开发入口。
- 采用 Monorepo 结构，将无 UI 依赖的纯逻辑 SDK (`@jcyao/print-sdk`) 与可视化设计器 (`designer`) 物理隔离，确保核心引擎可独立复用。
- 共享技术底座：子模块均基于 TypeScript 生态构建，SDK 使用 Rollup 打包以支持多格式导出，Designer 使用 Vite + React 提供交互界面，两者通过 npm 包引用或本地链接建立依赖关系。