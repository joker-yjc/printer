- 采用接口驱动设计，通过 `PipeExecutor` 接口（`types.ts`）统一定义管道行为。
- 核心逻辑位于 `registry.ts`，利用 `Map` 维护执行器注册表，并提供 `executePipe` 作为统一入口。
- `executors/` 目录下存放具体实现，分为复杂逻辑类（如 `DatePipe.ts`）和简单内联类（在 `executors/index.ts` 中定义）。
- 模块初始化时自动在 `registry.ts` 中完成所有内置管道的注册，实现即插即用。