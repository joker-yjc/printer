- 所有渲染器均实现统一的 `ComponentRenderer` 接口，通过 `render(component, context)` 方法返回 HTML 字符串。
- 管道系统采用注册中心模式，通过 `registerExecutor` 注册并在 `registry.ts` 中集中初始化内置管道。
- 分页计算采用虚拟流式布局，通过累加组件高度与间距（gap）判断换页，并对表格组件进行特殊的跨页拆分处理。
- 打印执行时统一使用 `waitForImagesLoaded` 确保外部资源就绪，并通过 `afterprint` 事件或超时兜底机制清理临时 `iframe`。