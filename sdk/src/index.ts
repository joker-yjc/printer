/**
 * 打印SDK入口文件
 * 导出所有公共API
 * 
 * SDK 设计原则：
 * - 完全解耦：不依赖任何外部服务
 * - 数据驱动：直接接收模板和数据
 * - 无状态：无需初始化和配置
 */

// 导出所有sdk功能（已包含 types 导出）
export * from './sdk';

// 导出管道系统（供设计器使用）
export * from './pipes';
